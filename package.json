{
  "name": "backoff-calc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "check": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "lighthouse": "^12.8.2"
  }
}
